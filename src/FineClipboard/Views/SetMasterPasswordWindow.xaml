<Window x:Class="FineClipboard.Views.SetMasterPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置主密码" Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize" Background="{DynamicResource PopupBackground}">
    <StackPanel Margin="18">
        <StackPanel x:Name="OldPanel" Visibility="Collapsed" Margin="0,0,0,10">
            <TextBlock Text="当前主密码" Foreground="#444" Margin="0,0,0,3" />
            <PasswordBox x:Name="OldBox" Padding="6,4" FontSize="13" />
        </StackPanel>

        <TextBlock Text="新主密码" Foreground="#444" Margin="0,0,0,3" />
        <PasswordBox x:Name="NewBox" Padding="6,4" FontSize="13" />

        <TextBlock Text="确认新主密码" Foreground="#444" Margin="0,10,0,3" />
        <PasswordBox x:Name="ConfirmBox" Padding="6,4" FontSize="13" />

        <TextBlock Text="⚠ 忘记主密码将无法找回已保存的密码,请牢记。"
                   Foreground="#A86" FontSize="11" TextWrapping="Wrap" Margin="0,12,0,0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" IsCancel="True" Width="84" Padding="0,5" />
            <Button Content="保存" IsDefault="True" Click="Save_Click" Width="84" Padding="0,5" Margin="8,0,0,0" />
        </StackPanel>
    </StackPanel>
</Window>
